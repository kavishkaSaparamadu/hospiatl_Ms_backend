const express = require('express');
const router = express.Router();
const doctorController = require('../controllers/doctor/detailsController'); // Import the doctor controller


// router.post('/user/register', registrationController.registerUser);

// // Login route
// router.post('/user/login', authController.login);

// // Authentication check route
// router.get('/user/authenticate', authController.isAuth);


// router.post('/doctor/apply', doctorController.applyDoctor);

module.exports = router;
